{
  "connect.name": "dbz.sales.order_items.Key",
  "fields": [
    {
      "default": 0,
      "name": "id",
      "type": {
        "connect.default": 0,
        "type": "long"
      }
    }
  ],
  "name": "Key",
  "namespace": "dbz.sales.order_items",
  "type": "record",
  "doc": "Ключ сообщения для sales.order_items — первичный ключ таблицы. По нему consumer части 2 делает MERGE/DELETE в MS SQL. Партиция всё равно одна, поэтому ключ на распределение не влияет."
}
