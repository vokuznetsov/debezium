{
  "connect.name": "dbz.catalog.products.Key",
  "fields": [
    {
      "default": 0,
      "name": "id",
      "type": {
        "connect.default": 0,
        "type": "long"
      }
    }
  ],
  "name": "Key",
  "namespace": "dbz.catalog.products",
  "type": "record",
  "doc": "Ключ сообщения для catalog.products — первичный ключ таблицы. По нему consumer части 2 делает MERGE/DELETE в MS SQL. Партиция всё равно одна, поэтому ключ на распределение не влияет."
}
